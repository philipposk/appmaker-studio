# ── AppMaker frontend environment ──────────────────────────────
# Copy to `.env`. Create React App only exposes REACT_APP_* vars, baked in
# at BUILD time. The anon key is public-safe — RLS protects every row.
# Never put the Supabase service-role key here.

# Shared 6x7 Supabase project.
REACT_APP_SUPABASE_URL=https://fmrnqepyyjucnfbrqawl.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-publishable-anon-key

# ── Legacy (standalone Express backend only — unused on the platform) ──
# Base URL of the old API. Leave unset for the Supabase/Vercel deployment.
# REACT_APP_API_URL=http://localhost:8000/api
