# AppMaker — Audit & Remediation Log

Post-migration audit (Supabase + Vercel, 6x7 platform) and the fixes applied on
branch `platform-fixes`. Severity reflects impact on the core product.

## Executive summary

- **The generate → view → refine loop was silently broken.** Generation saved
  files in a flat shape no UI component could read; after save the Code tab,
  Live Preview, Workflow graph and refine-context all came up empty. **Fixed.**
- **The database layer is healthy** (verified live): `appmaker.apps` /
  `appmaker.iterations` have RLS with correct owner-scoped policies, `anon` has
  no access, and the Supabase security advisor flags zero AppMaker issues. The
  schema is exposed to the API; authenticated users have full access. There was
  **no schema-exposure blocker** — an early assumption that proved false.
- **Build is green** throughout; every issue was a logic/architecture/perf
  defect, not a compile failure.

## Findings & status

| # | Sev | Finding | Status |
|---|-----|---------|--------|
| 1 | Critical | `generated_code` saved flat `{files}` but every reader expects nested `frontend.structure`/`backend.structure` → whole loop broken | ✅ Fixed — `lib/appFiles.ts` canonical builder/reader; save writes both shapes |
| 2 | Critical | SSO cookie `sb-6x7-auth` never actually written; hand-rolled adapter wouldn't match the hub | ✅ Fixed — `@supabase/ssr` `createBrowserClient`, standard cookie names, domain `.6x7.gr` |
| 3 | High | Dead axios `api.ts` reachable in Deploy/Test → hit `localhost:8000`, hard-redirected users out on 401 | ✅ Fixed — Supabase token, no redirect; Deploy/Test/Download rebuilt client-side |
| 4 | High | Session cookie >4KB silently dropped (no chunking) → intermittent logout for OAuth/rich users | ✅ Fixed — `@supabase/ssr` chunks automatically |
| 5 | High | `logout` ran async `signOut()` inside a sync reducer | ✅ Fixed — proper thunk, clears state on settle |
| 6 | High | `fetchApp` `select('*')` pulls entire `generated_code` blob every open | ⏳ Deferred — builder needs the data; real fix = move file bytes to Storage (see Build-new) |
| 7 | High | Heavy editors (monaco/grapesjs/reactflow) eagerly bundled | ✅ Fixed — code-split, main bundle 555kB→181kB; dead deps removed |
| 8 | High | WebContainer reinstalled npm on every change, leaked dev process | ✅ Fixed — shared singleton, install-cache, teardown |
| 9 | Medium | Boot race: `getMe()` + `onAuthStateChange` both drove `loading`; spinner could hang | ✅ Fixed — single source of truth + 8s safety timeout |
| 10 | Medium | Register gave no email-confirmation feedback | ✅ Fixed — "check your email" panel + error surfacing |
| 11 | Medium | `fetchApps` over-fetched jsonb for a list view | ✅ Fixed — trimmed select |
| 12 | Medium | Streaming spreads whole arrays into setState per token | ⏳ Deferred — render throttle (polish) |
| 13 | Medium | `App` typed `any` across builder; dual-shape `generatedCode` hid #1 | ◑ Partial — canonical `GeneratedCode` type in `lib/appFiles.ts`; full `any`→`App` sweep pending |
| 14 | Low | Session cookie long-lived, JS-readable, `.6x7.gr` scope → XSS blast radius | ⏳ Documented in DEPLOY.md; needs platform CSP decision |
| 15 | Low | `_id` vs `id` Mongo-era remap | ⏳ Deferred — cosmetic, 40+ sites |
| 16 | Low | Design tokens duplicated as raw hex | ⏳ Deferred — hygiene |

### Dismissed (verified false positives)
- `deleteCookie` omitting Secure/SameSite does **not** break deletion (RFC 6265:
  delete identity is name+Domain+Path).
- OAuth callback `<Navigate>` does **not** drop the session — exchange runs at
  module load; History API navigation can't interrupt it.

## Build-new (to finish the product)
- **Real deploy pipeline** (Vercel deploy hook / Edge Function) wired to `DeploymentPanel`.
- **File storage**: move generated bytes from `generated_code` jsonb → Supabase Storage (manifest in row, lazy per-tab fetch).
- **WebContainer node_modules snapshot** so previews skip cold installs.
- **Pagination** for Projects/Dashboard before app counts grow.
- **`app_access` tracking** so the platform "your apps" dashboard sees AppMaker usage.
- **Finish design-system migration**: remaining legacy `.scss` pages → design tokens.
